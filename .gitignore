# Compiled Power BI files are release artefacts, not source
*.pbix

# Power BI Desktop local state and caches (not part of the PBIP definition)
**/.pbi/
*.abf

# Fabric item descriptors emitted by Desktop on save (not needed for the PBIP source)
.platform

# OS noise
Thumbs.db
Desktop.ini
.DS_Store

# Working screenshots (optimised copies live in the website repo)
Screenshots/
